#ifndef __TEST_OVERALL_09562649_9C15_413C_81EA_6B6EE98E9CBB_H__
#define __TEST_OVERALL_09562649_9C15_413C_81EA_6B6EE98E9CBB_H__

#include <gtest/gtest.h>

void OpenTestScript( const char *filename );
void OverallTest(float fps = 25, int width=3840, int height=2160,
    double start=0, double end=60);

char directory[256];

/* Normal ASS scripts */
TEST(OverallTest, 01)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "01.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 02)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "02.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 03)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "03.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 04)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "04.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 05)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "05.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 06)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "06.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 07)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "07.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 08)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "08.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 09)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "09.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 10)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "10.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 11)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "11.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 12)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "12.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 13)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "13.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 14)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "14.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 15)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "15.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 16)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "16.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 17)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "17.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 18)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "18.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 19)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "19.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 20)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "20.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 21)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "21.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 22)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "22.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 23)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "23.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 24)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "24.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 25)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "25.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 26)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "26.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 27)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "27.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 28)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "28.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 29)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "29.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 30)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "30.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 31)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "31.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 32)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "32.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTest, 33)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "33.ass");
    OpenTestScript(numbuf);
    OverallTest();
}


/* High requirement ASS scripts */
TEST(OverallTestHigh, 01)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "high\\01.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

TEST(OverallTestHigh, 02)
{
    char numbuf[256];
    strncpy(numbuf, directory, 256);
    strcat(numbuf, "high\\02.ass");
    OpenTestScript(numbuf);
    OverallTest();
}

#endif // __TEST_OVERALL_09562649_9C15_413C_81EA_6B6EE98E9CBB_H__